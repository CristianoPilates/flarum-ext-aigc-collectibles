inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling
  nixpkgsPlaywright:
    # Pinned via devenv.lock to a nixos-25.11 revision that includes playwright-test and playwright-mcp.
    url: github:NixOS/nixpkgs/nixos-25.11
